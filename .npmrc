# Mantiene el árbol de node_modules estricto pero permite que las apps
# resuelvan dependencias de peers comunes (React, etc.) del workspace.
auto-install-peers=true
strict-peer-dependencies=false
shared-workspace-lockfile=true
